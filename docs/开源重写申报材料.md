# HexStrike Augment 开源重写申报材料

> **项目名称：** HexStrike Augment（`hexstrike_augment`）  
> **原软件：** HexStrike AI（[0x4m4/hexstrike-ai](https://github.com/0x4m4/hexstrike-ai)）  
> **重写仓库：** https://github.com/clutch-61/hexstrike_augment  
> **参考材料：** 《HexStrike 项目阶段性工作汇报》、寇-RAG 五周工程成果  

---

## 一、功能介绍

### 1.1 原软件（HexStrike AI）核心功能

HexStrike AI 是面向**授权渗透测试、CTF、红队演练**场景的安全自动化平台，通过 MCP 协议向大模型暴露安全工具能力。其核心能力包括：

| 类别 | 功能说明 |
|------|----------|
| **工具编排** | 通过 `hexstrike_mcp.py`（FastMCP）将 **100+** 安全工具封装为 MCP 工具，供 Agent 调用 |
| **后端服务** | `hexstrike_server.py` 提供 HTTP API（默认 `http://localhost:8888`），执行扫描、利用、信息收集等任务 |
| **场景支持** | 漏洞扫描、服务识别、Web/网络渗透辅助、CTF 解题工作流等 |
| **AI 对接** | 面向 Claude/Cursor 等 MCP 客户端；强调参数优化、日志与错误恢复 |
| **交互形态** | 以「工具执行引擎 + MCP 桥接」为主，**不内置**本地 Ollama 对话客户端与多 Agent 调度 |

原软件解决的核心问题：**让 AI 能调用专业安全工具**，但知识检索、本地模型、工具筛选与对话体验需依赖外部框架单独集成。

---

### 1.2 重写软件（HexStrike Augment）全部功能

HexStrike Augment 在保留 HexStrike 全部 MCP 工具能力的前提下，基于 **Ollama + ollmcp 客户端框架** 进行集成与扩展，形成「**对话客户端 + 多 Agent + 双路 RAG + HexStrike 工具链**」的一体化系统。

#### （1）继承自原软件（通过子模块 `mcp/`）

- HexStrike MCP 工具全集（`hexstrike-ai` 服务器）
- 与 HexStrike HTTP 后端的通信与任务执行逻辑
- 安全工具参数化调用与结果回传

#### （2）继承并增强自 ollmcp 基准客户端（`mcp_client_for_ollama/`）

- 多 MCP 服务器并行连接（stdio / SSE / HTTP）
- Ollama 本地/云端模型 tool calling、流式输出
- 工具/服务器动态启用、Human-in-the-Loop（HIL）确认
- 模型参数细调、上下文管理、性能指标展示
- 配置持久化、服务器热重载、模糊命令补全

#### （3）重写项目新增功能

| 模块 | 功能 |
|------|------|
| **自主多代理（`multi_agent/`）** | Strategy / Selector / Executor 三层架构；自然语言任务分解；工具从约 152 个筛选至约 8 个，降低上下文占用 |
| **Chroma RAG（`rag_knowledge_base/` + `rag_mcp_server.py`）** | 基于 ChromaDB + Ollama embedding 的轻量漏洞知识检索；`build_knowledge_base.py` 建库 |
| **HexStrike RAG（`hexstrike_rag/` + `hexstrike_rag_mcp_server.py`）** | 五周工程成果：BM25 + 向量 + RRF + Cross-Encoder 重排 + 规则过滤；Qdrant/Redis 可选；**`verify_payload_safety`** |
| **UltraRAG 集成（可选）** | `ultrarag_mcp_server.py` 对接 UltraRAG YAML 检索配置 |
| **统一配置与启动** | `*.example.json` 模板；`start_with_multi_agent.sh` / `.ps1` 一键启动 |
| **评估与调优工具** | `benchmark_v1.py`（Recall/nDCG/MRR）；`performance_tuner.py`（P50/P95）；`cache_layer_v2.py`（标签化缓存） |

#### MCP 工具总览（重写后）

| 服务名 | 工具示例 | 来源 |
|--------|----------|------|
| `hexstrike-ai` | 100+ 渗透/扫描类工具 | 原 HexStrike |
| `rag-knowledge-base` | `search_security_knowledge`、`get_knowledge_base_stats` | 重写（Chroma） |
| `hexstrike-rag` | 上述检索 + **`verify_payload_safety`** | 重写（增强 RAG） |

---

### 1.3 相较原软件的创新与优化

| 维度 | 原 HexStrike | HexStrike Augment 创新与优化 |
|------|--------------|------------------------------|
| **模型接入** | 依赖外部 MCP 客户端 | **内置 ollmcp + Ollama**，本地可离线使用 |
| **知识检索** | 无内置 RAG | **双路 RAG**：轻量 Chroma + 生产级 BM25/Qdrant 管线 |
| **检索质量** | — | 四/五步检索、重排、去重、查询扩展；Recall@10 **88%**、nDCG@10 **0.76**（见第二节） |
| **Agent 智能** | 工具直调 | **多 Agent 自主决策**，按任务动态选工具 |
| **Token 效率** | 全量工具上下文 | 筛选后单次约 **920 tokens** 工具描述，较全量 **~17,480** 节省约 **94.7%** |
| **延迟** | 取决于单次工具 | RAG 管线 P95 **185ms**（含缓存优化，见汇报材料） |
| **安全合规** | 工具执行风险提示 | 新增 **Payload 安全验证** MCP 工具 |
| **可扩展** | MCP 工具扩展 | 模块化 RAG、子模块 HexStrike、可选 UltraRAG |
| **开源协作** | 独立仓库 | 统一 `hexstrike_augment`，文档与子模块边界清晰 |

---

## 二、重写软件测试情况

### 2.1 测试原则

1. **不得引入恶意功能**：所有代码开源可审计；工具调用需用户显式启用；HIL 支持人工确认；`verify_payload_safety` 用于**降低误用风险**，不用于生成攻击载荷。  
2. **测试类型**：单元测试（检索/缓存/评估模块）、集成测试（MCP stdio + ollmcp）、场景测试（多 Agent + Mock RAG）。  
3. **测试环境**：Python 3.10+、Ollama、可选 Docker（Qdrant/Redis）。

### 2.2 单元测试与模块验证

| 模块 | 测试方式 | 结果摘要 |
|------|----------|----------|
| `hexstrike_rag/benchmark_v1.py` | 50/210 条标注查询，Recall@K、nDCG@K、MRR、MAP | Recall@10 **88%**，nDCG@10 **0.76**，MRR **0.83** |
| `hexstrike_rag/bm25_retriever.py` | CVE/端口/关键词分词召回 | 精确实体命中优于纯向量基线 |
| `hexstrike_rag/deduplication.py` | 精确+模糊双去重 | 去重率 **>95%**（数据层验收） |
| `hexstrike_rag/cache_layer_v2.py` | 命中率统计、标签失效 | 命中率 **60–80%**（稳态查询） |
| `hexstrike_rag/performance_tuner.py` | 分步耗时 P50/P95 | 见下表对比数据 |
| `mcp_client_for_ollama/multi_agent/` | 工具筛选、Mock 工具调用链路 | 8 工具子集可稳定加载与执行 |

### 2.3 集成测试

| 场景 | 方法 | 结果 |
|------|------|------|
| HexStrike RAG MCP × ollmcp | `HEXSTRIKE_USE_MOCK=true`，`ollmcp -j hexstrike_rag_config.json` | **15/15** 用例通过（第 5 周验收） |
| 三工具可用性 | `search_security_knowledge` / `get_knowledge_base_stats` / `verify_payload_safety` | Mock 模式下全部可调用 |
| HexStrike + 双 RAG | `hexstrike-with-rag-config.json` 三服务并联 | 客户端可发现并选择性启用工具 |
| 多代理模式 | 客户端输入 `ma`，自然语言任务 | Strategy→Selector→Executor 闭环可跑通 |

### 2.4 性能对比实验数据

以下数据来自项目阶段性汇报中的 **RAG 检索层** 优化实验（同一硬件环境、10 万条安全语料、210 条测试查询）。

#### （1）检索延迟优化

| 指标 | 优化前（第 3 周初） | 优化后（第 3–4 周） | 提升 |
|------|---------------------|---------------------|------|
| P50 延迟 | 265 ms | **95 ms** | **↓64%** |
| P95 延迟 | 420 ms | **180 ms**（验收 **185 ms**） | **↓57%** |
| 缓存命中率 | 0% | **60–80%** | 显著降低重复查询开销 |

#### （2）检索质量指标

| 指标 | 目标 | 实际 | 是否达标 |
|------|------|------|----------|
| Recall@10 | ≥85% | **88%** | ✅ |
| nDCG@10 | ≥0.75 | **0.76** | ✅ |
| MRR | ≥0.75 | **0.83** | ✅ |
| 知识库规模 | ≥100,000 条 | **100,000** | ✅ |

#### （3）多 Agent 工具上下文 Token 对比（ollmcp 客户端）

| 模式 | 工具数量（约） | 工具描述 Token（约） | 说明 |
|------|----------------|----------------------|------|
| 传统全量加载 | 152 | **17,480** | 基线：每工具约 115 tokens |
| 多 Agent 筛选后 | 8 | **920** | Selector 按任务选子集 |
| **节省比例** | — | **≈94.7%** | 支持更多轮次自主决策 |

#### （4）相对原 Chroma 单路 RAG 的扩展收益（架构层）

| 对比项 | 原 `rag_knowledge_base`（Chroma） | 新 `hexstrike_rag` |
|--------|-----------------------------------|---------------------|
| 检索方式 | 纯向量 | BM25 + 向量 + RRF + 重排 |
| 存储 | 单机 Chroma | Qdrant（可水平扩展） |
| CVE 精确匹配 | 较弱 | BM25 强化 |
| 时效/规则过滤 | 无 | 有 |
| 去重 | 无专门模块 | 双策略去重 >95% |

### 2.5 安全与合规说明

- 子模块 HexStrike 工具**仅用于授权测试**；README 与文档中已声明合规使用范围。  
- 仓库已清理历史中的 API Key、个人路径、靶场 flag 等敏感文件（`git filter-repo`）。  
- 无后门、无远程控制、无隐蔽数据外传逻辑；依赖均为公开 PyPI / 子模块。

---

## 三、文档说明

### 3.1 环境准备

| 依赖 | 版本/说明 |
|------|-----------|
| Python | 3.10+ |
| Git | 支持 submodule |
| Ollama | 本地服务，建议 `qwen3` 等支持 tool calling 的模型 |
| Docker | 可选，Qdrant `6333`、Redis `6379` |

### 3.2 安装步骤

```bash
# 1. 克隆（含 HexStrike 子模块）
git clone --recursive https://github.com/clutch-61/hexstrike_augment.git
cd hexstrike_augment

# 2. Python 依赖
pip install mcp rich typer httpx ollama
pip install -r mcp/requirements.txt
pip install -r hexstrike_rag_requirements.txt

# 3. 复制配置模板（将 <PROJECT_ROOT> 改为本机绝对路径）
cp hexstrike-with-rag-config.example.json hexstrike-with-rag-config.json
# Windows PowerShell:
# Copy-Item hexstrike-with-rag-config.example.json hexstrike-with-rag-config.json

# 4. 拉取 Ollama 模型
ollama pull qwen3:32b
ollama pull nomic-embed-text   # 仅构建 Chroma 知识库时需要
```

### 3.3 启动 HexStrike 后端（使用渗透工具时）

```bash
cd mcp
pip install -r requirements.txt
python hexstrike_server.py
# 服务默认 http://localhost:8888
```

### 3.4 启动 ollmcp 客户端

**Linux / macOS：**

```bash
chmod +x start_with_multi_agent.sh
./start_with_multi_agent.sh
```

**Windows：**

```powershell
.\start_with_multi_agent.ps1
```

**手动启动：**

```bash
python -m mcp_client_for_ollama.cli \
  --servers-json hexstrike-with-rag-config.json \
  --model qwen3:32b
```

### 3.5 基本使用方法

| 步骤 | 操作 |
|------|------|
| 1 | 启动后输入 `tools` 查看已连接 MCP 服务 |
| 2 | 输入 `a` 启用全部工具，或按编号选择性启用 |
| 3 | 输入 `s` 保存并返回对话 |
| 4 | 用自然语言提问，例如：「查询 CVE-2020-17519 的利用思路」 |
| 5 | 输入 **`ma`** 进入**自主多代理模式**，适合复杂渗透分析任务 |
| 6 | 输入 `hil` 可开关人工确认（执行工具前确认） |

### 3.6 构建 Chroma 知识库（可选）

```bash
python build_knowledge_base.py \
  --docs-dir ./data/extracted_readmes \
  --rebuild --test
```

### 3.7 启用 HexStrike RAG 真实管线（可选）

1. 启动 Qdrant：`docker run -d -p 6333:6333 qdrant/qdrant`  
2. 配置 `hexstrike_rag_config.json` 中 `HEXSTRIKE_USE_MOCK=false`  
3. 按 [HEXSTRIKE_INTEGRATION_GUIDE.md](../HEXSTRIKE_INTEGRATION_GUIDE.md) 导入语料  

### 3.8 文档索引

| 文档 | 路径 |
|------|------|
| 项目总览 | [README.md](../README.md) |
| HexStrike RAG 集成 | [HEXSTRIKE_INTEGRATION_GUIDE.md](../HEXSTRIKE_INTEGRATION_GUIDE.md) |
| Chroma RAG | [RAG_KNOWLEDGE_BASE_README.md](../RAG_KNOWLEDGE_BASE_README.md) |
| UltraRAG | [ULTRARAG_INTEGRATION.md](../ULTRARAG_INTEGRATION.md) |

---

## 四、重写软件代码

### 4.1 代码托管与获取

- **GitHub：** https://github.com/clutch-61/hexstrike_augment  
- **HexStrike 上游子模块：** https://github.com/0x4m4/hexstrike-ai（路径 `mcp/`）

```bash
git clone --recursive https://github.com/clutch-61/hexstrike_augment.git
```

### 4.2 代码规模统计（不含注释与空行）

统计口径：Python 源文件，剔除以 `#` 开头的整行注释与空白行；`mcp/hexstrike-env` 等虚拟环境**不计入**。

| 目录/文件 | 有效代码行数 | 性质 |
|-----------|-------------|------|
| `mcp_client_for_ollama/` | **5,377** | 客户端 + 多 Agent（含 ollmcp 基准扩展） |
| `hexstrike_rag/` | **3,837** | **新编写** RAG 检索与服务支撑 |
| `mcp/`（子模块，跟踪的 .py） | **18,209** | **原 HexStrike** 核心（继承） |
| `rag_knowledge_base/` | **389** | **新编写** Chroma RAG |
| `ultrarag_integration/` | **252** | **新编写** 可选集成 |
| 根目录 MCP 服务与脚本 | **~875** | **新编写** 胶水层 |
| **合计（全仓库）** | **≈28,939** | **远超 2000 行要求** |

### 4.3 新编写代码占比说明

按「重写贡献」口径（不计入上游 HexStrike 子模块 `mcp/` 内原有代码）：

| 类别 | 行数（约） |
|------|-----------|
| 新编写模块（`hexstrike_rag` + `rag_knowledge_base` + `ultrarag` + MCP 入口 + 配置脚本） | **≈5,400** |
| 在 ollmcp 基准上新增/改写（`multi_agent/` 等） | **≈1,300+** |
| 客户端其余定制 | **≈1,000+**（估算） |
| **新编写合计** | **≈7,700+** |
| **重写项目总行数（不含子模块原码）** | **≈10,730** |
| **新编写占比** | **≈72%**（满足 **≥70%** 要求） |

> 说明：`mcp_client_for_ollama` 基于 [jonigl/mcp-client-for-ollama](https://github.com/jonigl/mcp-client-for-ollama) 扩展；**多 Agent、RAG 双路、HexStrike 集成配置与 MCP 封装**为本项目新编写与集成部分。原 HexStrike 以 **git submodule** 方式引用，不重复计入新编写行数。

### 4.4 核心新编写目录说明

```
hexstrike_rag/                 # 五周 RAG 成果（BM25/向量/重排/缓存/评估）
hexstrike_rag_mcp_server.py    # ollmcp 兼容 MCP 入口
mcp_client_for_ollama/multi_agent/  # 自主多代理
rag_knowledge_base/            # Chroma 知识库
rag_mcp_server.py              # Chroma MCP 入口
build_knowledge_base.py        # 建库脚本
ultrarag_integration/          # UltraRAG 桥接
```

### 4.5 许可证与归属

- ollmcp 客户端：遵循上游 [mcp-client-for-ollama](https://github.com/jonigl/mcp-client-for-ollama) 许可证  
- HexStrike：遵循 [hexstrike-ai](https://github.com/0x4m4/hexstrike-ai) 许可证  
- 本项目扩展代码：见仓库 `README.md` 致谢与说明  

---

## 五、贡献者贡献记录

### 5.1 贡献统计截图

以下为 GitHub 仓库 **Insights → Contributors** 页面截图（统计区间：2025-12-28 至 2026-05-24，仓库：https://github.com/clutch-61/hexstrike_augment ）。

> **说明：** 请将 Contributors 页面完整截图插入此处（含顶部「Commits over time」折线图及下方各贡献者明细表）。

![GitHub Contributors 贡献统计截图](./assets/github-contributors.png)

*（若申报材料为 Word/PDF，请直接粘贴您保存的 Contributors 截图。）*

---

### 5.2 截图数据摘要

根据 GitHub 贡献统计页面，本仓库共有 **3 名贡献者**、**7 次提交**，累计新增代码约 **19,055 行**（含删除约 693 行，为历史清理与文档精简所致）。提交在时间轴上呈两阶段分布：2026 年 1 月初完成项目基线导入，2026 年 5 月下旬完成集成、隐私清理、RAG 模块与文档体系建设。

| 排名 | 贡献者 | 提交次数 | 新增行数 | 删除行数 | 主要贡献阶段 |
|------|--------|----------|----------|----------|--------------|
| 1 | **clutch-61** | 5 | +8,360 | −685 | 2026 年 5 月（仓库维护与发布） |
| 2 | **TiamJay** | 1 | +10,593 | 0 | 2026 年 1 月（项目基线导入） |
| 3 | **pc** | 1 | +102 | −8 | 2026 年 5 月（本地配置与协作提交） |

---

### 5.3 各贡献者文字说明

#### （1）clutch-61（仓库所有者，主要维护者）

- **角色：** `hexstrike_augment` 开源仓库创建者与主要维护者（GitHub 账号：clutch-61）。
- **提交内容（5 次）：**
  1. 合并远程仓库历史，解决 `.gitignore`、`rag_config.py` 等冲突；
  2. 移除敏感路径与个人配置（`123.txt`、`docker_ps.txt`、含 API Key 的 `.continue/` 等），完善 `.gitignore` 与 `*.example.json` 模板；
  3. 使用 `git filter-repo` 重写 Git 历史，清除历史中的隐私信息与旧路径；
  4. 集成寇-RAG 五周成果（`hexstrike_rag/`、`hexstrike_rag_mcp_server.py`），重写项目 `README.md`；
  5. 新增《开源重写申报材料》等文档。
- **贡献价值：** 完成从「内部/分散代码」到「可公开发布仓库」的治理；建立子模块引用 HexStrike 原软件；形成双 RAG + 多 Agent + ollmcp 的一体化交付形态。

#### （2）TiamJay（项目基线贡献者）

- **角色：** 项目首次代码基线的提交者。
- **提交内容（1 次）：** `first commit`，一次性导入约 **10,593 行** 核心代码，包括：
  - `mcp_client_for_ollama/`（含多代理模块）；
  - `rag_knowledge_base/` 与 `rag_mcp_server.py`；
  - `ultrarag_integration/`、构建脚本及 MCP 配置骨架；
  - 与 ollmcp / HexStrike 集成相关的基础目录结构。
- **贡献价值：** 奠定重写软件的代码主体，使后续功能扩展与开源发布具备可运行的基础框架。

#### （3）pc（协作与本地化贡献者）

- **角色：** 本地开发环境与协作提交参与者。
- **提交内容（1 次）：** 约 **+102 / −8** 行，主要为 Windows 路径下的配置微调、文档或脚本的小幅修订（与 clutch-61 5 月集中提交时段一致）。
- **贡献价值：** 补充本机可运行配置，支持 Windows 环境下克隆、配置与联调验证。

---

### 5.4 提交时间线说明（对应截图折线图）

| 时间段 | 提交数 | 说明 |
|--------|--------|------|
| 2026-01-04 前后 | 1 | TiamJay 完成 `first commit`，导入项目主体代码（折线图左侧小峰） |
| 2026-01 至 2026-05 中旬 | 0 | 无新的 GitHub 提交记录（本地开发与汇报材料整理阶段） |
| 2026-05-24 前后 | 6 | clutch-61 与 pc 完成开源治理、RAG 集成、README/申报材料更新并推送（折线图右侧主峰，共 6 次提交） |

上述分布说明：项目先完成**基线搭建**，再经过**功能完善与开源合规整理**后集中发布至 `clutch-61/hexstrike_augment`，与阶段性汇报（寇-RAG 五周成果、HexStrike 集成）时间线一致。

---

### 5.5 与代码规模统计的对应关系

Contributors 页面统计的 **+19,055 行** 为 Git 累计新增行数（含文档、配置与子模块指针变更）；与 cloc 等工具统计的 **约 13,234 行当前仓库体量** 口径不同，但均可证明：

1. 重写软件代码量 **远高于 2,000 行** 要求；
2. 贡献来源 **清晰可追溯**，无匿名或不可审计提交；
3. 近期提交以 **删除敏感信息、补充文档与模块** 为主，体现开源合规意识。

---

## 六、其他佐证材料

### 6.1 开源许可证与第三方合规说明

#### 6.1.1 本项目许可证策略

HexStrike Augment 采用 **分层许可** 方式：自研扩展代码统一适用 **MIT License**；引用的上游项目保留其原有许可证，通过 **Git 子模块** 或 **文档致谢** 方式区分权属，避免许可证混用冲突。

| 组成部分 | 来源仓库 | 许可证 | 与本项目关系 |
|----------|----------|--------|--------------|
| `mcp_client_for_ollama/` | 基于 [jonigl/mcp-client-for-ollama](https://github.com/jonigl/mcp-client-for-ollama) 扩展 | **MIT** | 保留上游许可；多 Agent 等增量模块随本仓库发布 |
| `mcp/`（子模块） | [0x4m4/hexstrike-ai](https://github.com/0x4m4/hexstrike-ai) | **MIT** | 子模块引用，不将原仓库代码并入本仓库历史 |
| `hexstrike_rag/`、`rag_knowledge_base/` 等 | 本项目自研（寇-RAG 五周成果等） | **MIT**（拟适用） | 重写核心增量代码 |
| 文档与配置模板 | 本项目 | MIT / CC 友好 | `README.md`、`*.example.json` 等 |

> **佐证：** 上游 ollmcp、HexStrike 官方仓库 GitHub License API 均为 `spdx_id: MIT`。本仓库根目录提供 `LICENSE` 与 `THIRD_PARTY_NOTICES.md`（见仓库文件）。

#### 6.1.2 第三方依赖许可证兼容性分析

对本项目 **直接依赖**（`pip install` 层面）进行梳理，结论：**无 GPL/AGPL 类“传染性”许可证冲突**（与自研 MIT 代码可共存）。

| 依赖类别 | 典型组件 | 常见许可证 | 兼容性说明 |
|----------|----------|------------|------------|
| MCP 协议 SDK | `mcp` | MIT | 与 MIT 完全兼容 |
| 客户端 UI | `rich`、`typer`、`httpx` | MIT | 兼容 |
| 本地 LLM | `ollama`（Python 库） | MIT | 兼容 |
| 向量/RAG（可选） | `chromadb` | Apache-2.0 | 允许在 MIT 项目中使用，需保留 NOTICE |
| 检索（可选） | `rank-bm25`、`sentence-transformers` | Apache-2.0 / BSD | 兼容 |
| 向量库（可选） | `qdrant-client` | Apache-2.0 | 兼容 |
| 缓存（可选） | `redis` 客户端 | MIT | 兼容 |

**特别说明（HexStrike 子模块）：**

- 子模块 `hexstrike-ai` 自身依赖较多（含安全工具链），其 `requirements.txt` 中个别组件（如部分安全框架）可能采用 **GPL** 等许可证。
- 上述依赖 **仅在使用者启用 HexStrike 子模块并安装其子模块环境时生效**，不作为本仓库 `hexstrike_augment` 根目录的强制捆绑发布内容。
- 合规建议：将 HexStrike 作为 **独立子模块/独立服务** 运行；本仓库扩展层仅通过 MCP 协议调用，**不在 MIT 扩展代码中静态链接或合并 GPL 代码**。

#### 6.1.3 许可证冲突结论（书面声明）

经对 **上游仓库许可证**、**本仓库自研模块** 及 **直接 Python 依赖** 的核对：

1. **未发现** MIT 与 Apache-2.0 之间的许可证冲突（满足常见开源组合规范）；
2. **未发现** 本仓库自研代码与 ollmcp、HexStrike 上游 MIT 许可之间的冲突；
3. 对 HexStrike 子模块中可能存在的 GPL 类工具链，已通过 **子模块隔离 + 运行时调用** 方式规避许可证传染风险；
4. 仓库已清理历史中的密钥、个人路径等敏感信息，符合开源发布合规要求。


### 6.2 权威认可与阶段性验收材料

截至本申报日，本项目 **未持有** 已授权的发明专利或软件著作权登记证书。以下材料作为 **与核心功能相关的阶段性成果与验收佐证**：

#### 6.2.1 阶段性工作汇报（项目组内部验收）

| 材料名称 | 内容摘要 | 与核心功能关联 |
|----------|----------|----------------|
| 《HexStrike 项目阶段性工作汇报》 | 五周 RAG 工程建设路线、里程碑 M1–M4、指标达成情况 | 对应 `hexstrike_rag/` 检索管线与 MCP 服务层 |
| 知识库规模验收 | 10 万条安全语料入库 | 数据层核心指标 |
| 检索质量验收 | Recall@10 **88%**、nDCG@10 **0.76**、MRR **0.83** | 检索层核心指标 |
| 性能验收 | P95 延迟 **185ms**（目标 &lt;500ms） | 性能优化成果 |
| MCP 集成验收 | **15/15** 测试用例通过 | 服务层与 ollmcp 对接 |

> **说明：** 请将《HexStrike项目阶段性工作汇报》PDF 关键页（指标表、架构图、里程碑页）截图或作为附件一并提交。

#### 6.2.2 开源发布与可审计性佐证

| 佐证类型 | 说明 |
|----------|------|
| **公开 GitHub 仓库** | https://github.com/clutch-61/hexstrike_augment ，代码、文档、提交记录可公开审计 |
| **贡献者统计** | 见第五节 Contributors 截图，提交来源清晰 |
| **子模块溯源** | `.gitmodules` 明确指向 `hexstrike-ai` 官方仓库 |
| **安全治理记录** | 使用 `git filter-repo` 清除历史敏感信息，体现开源合规意识 |

#### 6.2.3 技术_benchmark 与模块自证（代码内可验证）

| 模块 | 文件 | 作用 |
|------|------|------|
| 检索质量评估 | `hexstrike_rag/benchmark_v1.py` | Recall/nDCG/MRR/MAP 可复现评估 |
| 性能分析 | `hexstrike_rag/performance_tuner.py` | P50/P95 分步耗时统计 |
| 缓存评估 | `hexstrike_rag/cache_layer_v2.py` | 命中率与标签化失效策略 |

审查人员可在克隆仓库后，按 `HEXSTRIKE_INTEGRATION_GUIDE.md` 复现 Mock 模式联调；按集成指南启用真实管线后复现 Benchmark 流程。

#### 6.2.4 专利 / 证书情况说明（如实陈述）

| 类型 | 状态 | 说明 |
|------|------|------|
| 发明专利 | **无** | 未申请；创新点体现在工程集成与 RAG 管线实现 |
| 软件著作权 | **无** | 未登记；以 GitHub 开源发布作为主要权属与传播方式 |
| 第三方认证（等保、商密等） | **无** | 不适用本阶段实验室/课程/开源申报场景 |
| 行业竞赛 / 官方奖项 | **无**（如后续获得可补充） | 当前以阶段汇报指标达标为主要认可依据 |

---

### 6.3 建议一并提交的附件清单

| 序号 | 附件名称 | 用途 |
|------|----------|------|
| 1 | GitHub Contributors 截图 | 第五节已列 |
| 2 | 上游 `mcp-client-for-ollama`、`hexstrike-ai` 的 License 页面截图 | 证明 MIT 来源 |
| 3 | 本仓库 `LICENSE`、`THIRD_PARTY_NOTICES.md` 截图或文本 | 证明本项目许可声明 |
| 4 | 《HexStrike项目阶段性工作汇报》PDF 节选（指标表 + 架构图） | 核心功能验收佐证 |
| 5 | cloc / 代码统计截图（约 13,234 行） | 代码规模佐证 |
| 6 | ollmcp 联调成功界面截图（可选） | 功能可运行佐证 |

---

### 6.4 书面声明（可原样用于申报表）

本单位/本人郑重声明：HexStrike Augment 重写软件在发布过程中已对上游开源组件（mcp-client-for-ollama、hexstrike-ai 等）的 MIT 许可证进行核对；自研扩展模块拟采用 MIT 许可证；直接 Python 依赖均为 MIT、Apache-2.0、BSD 等 permissive 许可，**不存在已知的许可证冲突**。项目代码已公开于 GitHub，不含故意植入之恶意功能；渗透测试相关工具仅用于授权安全研究场景。截至申报日，**无相关专利证书**，阶段性工作汇报及 Benchmark 指标作为核心功能实现与验收的辅助佐证材料。

---

## 附录：与原软件关系示意图

```
┌─────────────────────────────────────────────────────────┐
│              HexStrike Augment（重写软件）                 │
├─────────────────────────────────────────────────────────┤
│  mcp_client_for_ollama  ← ollmcp 扩展 + 多 Agent【新】     │
│  hexstrike_rag/         ← 五周 RAG 管线【新】              │
│  rag_knowledge_base/    ← Chroma RAG【新】                 │
│  MCP 配置/启动脚本       ← 集成层【新】                     │
├─────────────────────────────────────────────────────────┤
│  mcp/ (submodule)       ← HexStrike AI 原软件【继承】      │
│    ├── hexstrike_mcp.py                                 │
│    └── hexstrike_server.py                              │
└─────────────────────────────────────────────────────────┘
```

---

*文档版本：v1.2 | 对应仓库提交：main 分支最新版 | 已补充第五、六节*
